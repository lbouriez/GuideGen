/**
 * Legacy generation phase exports - DEPRECATED
 *
 * This module is kept for backward compatibility.
 * New code should use the split phases in:
 * - ../guidelines/
 * - ../indexes/
 * - ../claude-artifacts/
 */

export { runGenerationPhase, writeArtifacts } from './generation';
